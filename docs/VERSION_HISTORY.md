# VERSION_HISTORY

## V1.3.8 competition评审候选（2026-07-04，未发布）

* 基线：`feature/modularization-stage-3`完成后的V1.3.8。
* 开发分支：`feature/competition-review-mode`。
* 在同一程序内新增统一`APP_MODE`，默认production，competition为比赛评审部署模式。
* 新增独立评审体验认证、评审只读管理认证、纯虚构演示数据和互斥存储适配。
* competition体验写入独立目录，不读取或写入正式运行记录，不连接Supabase。
* 学院流程改为课前完成页、模拟训练完成页、课后结果页、SUS/教学体验和全流程完成页连续衔接。
* 学院操作按钮使用按动作ID映射的短标题；操作历史前端倒序；手动结束增加二次确认；侧栏和按钮紧凑化。
* 候选稳定性收口新增学院“配合完成给药”展示适配，不修改情景动作ID；等待老师和其他非终末错误后仍可纠正。
* 用户可见模拟时间统一为`mm:ss`；课前/课后考试自动结束，模拟训练完成核心节点后由学习者二次确认结束；重启确认、刷新恢复和UI幂等行为统一。
* 训练完成页不再显示内部空值；三阶段结果页标题、问卷提交按钮和全流程完成侧栏状态改为自然中文。
* 新增正式入口契约、双App部署边界和统一候选验证脚本。
* 自动测试由224项先扩展为256项、288项，再扩展为296项并全部通过；4份情景JSON、12条黄金路径和黄金快照保持不变。
* 当前仅为开发候选，未发布、未部署、未连接正式Supabase、未修改正式数据库。
