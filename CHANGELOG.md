# CHANGELOG.md

## Unreleased / 开发中

### Security

* 建立根目录Git忽略规则，排除`working/`、原始包、发布包、虚拟环境、Secrets、缓存、JSONL、恢复草稿、日志和测试残留。
* 将正式目录示例认证及Supabase配置值置空，并移除README中的旧默认访问码说明。
* 对48个拟纳入Git的文件完成敏感信息终检，未发现真实凭据、真实个人数据、Secrets文件或本机绝对路径。
* 在开发副本中移除普通访问码和管理员密码的可用默认兜底。
* 必要Secrets缺失、为空、仅含空格、类型无效、两值相同或读取异常时，应用显示非敏感配置错误并安全停止；同名环境变量不能替代缺失的Secrets。
* 普通访问码和管理员密码使用集中读取与常量时间比较。
* 管理端授权上下文改为显式角色、机构类型、不可变机构ID和进程内签名；缺失、空白、非法、未知或篡改scope默认拒绝。
* 平台管理员只能由正确平台管理员密码路径显式建立；单位管理码不能声明或提升为平台管理员。
* 本地数据读取、问卷合并、统计、明细、CSV和JSONL导出在函数边界强制机构ID权限，直接调用不能绕过页面过滤。
* 移除单位管理码JSON明文兼容和旧式无盐SHA-256验证；单位凭据改用身份绑定PBKDF2-HMAC-SHA256、随机盐与恒定时间摘要比较。
* 单位凭据缺少盐、摘要、角色、机构类型或机构ID，身份不匹配，参数不足或出现多机构重复匹配时一律拒绝。

### Changed

* 校准模块化路线图与交付批次的对应关系：保留PR #2、#3、#4的历史名称和测试记录，将流程策略拆分记为原路线图阶段2的补完；原阶段3恢复为应用流程与会话恢复拆分，原阶段4固定为认证、存储、问卷和管理服务拆分。
* 新增临床训练、临床考核、学院训练和学院考核4个不可变流程策略，统一声明模式标识、反馈、评分呈现、错误处理、结束、结果页、问卷和会话恢复规则。
* 将系统模式元数据、6个阶段定义和阶段任务从入口文件移入纯Python策略模块；原阶段顺序、脚本角色和页面任务文字保持不变。
* 模拟器通过流程策略启用训练提示和考试动作随机，Streamlit入口通过策略处理即时反馈、实时评分、中性标签、手动完成、结果页和问卷衔接。
* 草稿保持`schema_version = 1`，恢复时增加可推导的流程策略身份核对，不改变旧草稿兼容路径。
* 建立4情景统一注册表和纯Python加载器，集中维护情景ID、脚本角色、文件名、系统模式、阶段映射、顺序和学院情景库ID。
* 应用入口改为通过注册表和加载器获取情景，不再扫描情景目录或从分散定义自行决定阶段脚本；引擎通用文件加载入口委托统一加载器。
* 恢复草稿只接受已登记情景路径并复核情景身份；原有JSON及可选YAML文件加载兼容行为保留。
* 建立模块化重构行为契约，明确场景加载、节点推进、反馈、评分、生命体征、结束、结果页、恢复、问卷、幂等保存和机构权限的不可变边界。
* 明确当前场景为`baseline + actions + dynamics + end_conditions`状态机，并为未来显式节点图定义唯一ID、目标引用、可达性、终止节点和允许循环契约。
* 建立`app/`作为正式Git开发目录；`working/V1.3.8_dev/`保留为迁移前开发快照，不再继续修改。
* 将开发副本当前系统版本语义最小统一为V1.3.8。
* 在包元数据中建立唯一运行时系统版本来源，应用版本常量引用该来源。
* README当前标题与版本定位、页面角标、侧边栏和管理端标题统一为V1.3.8。
* Python包版本统一为1.3.8；病例、schema和历史版本按原语义保留。
* 单位身份和新训练记录使用`role`、`organization_type`与`organization_id`；机构名称仅用于展示。
* 配置了机构列表时，普通登记页从可信列表选择机构并保存机构ID；未绑定ID的历史记录对单位管理员默认不可见。
* 单位管理员哈希配置可由Streamlit Secrets的`ORG_ACCESS_RECORDS`或本机配置提供；真实`config/org_access_codes.json`已排除Git。
* 新增不导入业务应用的本机CLI，使用`getpass`读取管理码，仅输出JSON或TOML哈希配置。

### Fixed

* 修复临床/学院、训练/考核过程中刷新页面会丢失当前进度的问题。
* 使用随机恢复标识、本机临时JSON草稿、客户端特征摘要、校验和与12小时有效期恢复病例、分支、操作、评分、反馈和流程阶段。
* 无效、损坏、过期或绑定不匹配的草稿安全返回入口；临床完成后转为完成态草稿，主动退出、返回首页或重置后清理。
* 恢复草稿不保存访问码、管理员密码、Secrets或管理员权限状态；`.runtime/`已排除版本控制。
* 修复临床训练和临床考核完成保存后立即返回登记页、无法显示结果页的问题。
* 临床结果页显示模式、任务、病例、完成状态、总分、模块评分、关键操作和问题反馈，并提供后续流程、重新开始和返回首页入口。
* 完成态结果支持刷新恢复；同一会话重复完成不会重复生成报告或追加结果记录，后续流程异常时保留结果。
* 修复学院课后问卷失败可能导致训练结果与问卷状态丢失的问题；训练结果现在先可靠保存，再进入问卷。
* 问卷失败时保留答案草稿和待提交状态，刷新或同一浏览器在进程重启后可恢复，并提供不误报成功的安全重试。
* 为本地训练和问卷JSONL增加随机稳定标识、跨进程文件锁、幂等追加、磁盘同步和损坏行告警。
* 问卷以独立JSONL事件保存，历史与导出读取时按完成标识合并，避免重试时重复保存训练结果。
* 修复后台已解锁但scope缺失时回退平台管理员的角色提升漏洞。
* 修复单位权限仅在页面层过滤、读取函数和导出函数可返回全量数据的问题。
* 修复异常单位管理码角色、空scope和机构名称授权造成的权限边界不可靠问题。
* 删除后台生成、暂存和显示明文单位管理码的路径，不再恢复旧式明文兼容。

### Tests

* 新增27项流程策略契约测试，覆盖4种流程、6个阶段、策略唯一性、原工作流元数据、提示、评分、错误处理、结束、结果页、问卷衔接和恢复规则。
* 原有197项测试继续通过，当前合计224/224；12条黄金快照、4份情景JSON、82个动作和44条动态规则保持一致。
* 模块化第3阶段未连接Supabase或其他外部业务服务，测试后无临时Secrets、缓存、JSONL、日志或业务数据残留。
* 新增22项情景注册表与加载器专项测试，覆盖唯一性、6组阶段映射、按ID/角色/阶段加载、路径约束、元数据核对、未知输入拒绝和入口路由。
* 原有175项测试继续通过，当前合计197/197；4份情景、12条黄金路径、82个动作、44条动态规则、情景文件SHA-256和黄金JSON快照均保持一致。
* 模块化第2阶段未连接Supabase或其他外部业务服务，测试后无临时Secrets、缓存、JSONL、日志或业务数据残留。
* 新增20项场景结构契约测试，覆盖4份场景、82个动作选项、44条动态规则、结束条件、评分维度、生命体征结构及未来显式图约束。
* 新增16项黄金行为测试，为临床训练、临床考核、学院训练和学院考核建立理想、典型错误和关键分支共12条规范化JSON快照。
* 原有139项测试继续通过，新增36项测试全部通过，当前合计175/175；4份场景文件SHA-256和只读基线28/28保持不变。
* 黄金快照不包含时间戳、随机ID、会话标识、本机路径或Secrets；测试后无临时Secrets、缓存、JSONL、日志或业务数据残留。
* 在正式目录`app/`使用既有独立虚拟环境执行全部139项测试，139/139通过；测试后无Secrets、缓存、日志或业务数据残留。
* 复核只读基线28/28文件大小和SHA-256与登记一致。
* 新增20项默认凭据安全测试，全部通过。
* 执行6项现有烟雾测试，全部通过。
* 完成本地离线Streamlit、普通登录、管理员登录和双模式入口回归。
* 确认无Supabase连接、无业务数据写入，测试进程已终止且端口已释放。
* 新增7项版本一致性测试，全部通过；当前Python测试合计33/33通过。
* 完成V1.3.8页面显示、README、包元数据、历史文档和病例/schema版本回归。
* 新增13项训练恢复专项测试；原有49项测试继续通过，当前合计62/62通过。
* 完成localhost临床训练真实刷新恢复、草稿清理、无Supabase连接和基线28/28完整性验证。
* 新增15项临床结果页专项测试；原有62项测试继续通过，当前合计77/77通过。
* 完成Streamlit AppTest结果页组件、完成态重复运行、localhost健康检查和无测试数据残留验证。
* 新增10项学院问卷恢复与本地JSONL幂等专项测试；原有77项测试继续通过，当前合计87/87通过。
* 完成两个独立进程争用同一完成标识和问卷提交标识、8进程并发解析、损坏行容错、进程重启恢复、无Secrets落盘和基线28/28完整性验证。
* 新增32项单位授权隔离测试；原有87项测试继续通过，当前合计119/119通过。
* 原32项单位隔离场景修复后32项通过、0项失败、0项无法验证；确认无外部连接、无测试数据残留且只读基线28/28完整。
* 新增20项单位管理码加盐哈希安全测试；原有119项测试继续通过，当前合计139/139通过。
* 完成明文配置、旧无盐SHA-256、缺失参数、跨机构身份、角色提升、重复管理码、CLI输出和Secrets加载回归。

### Notes

* 本地Git已初始化并配置私有GitHub远程；模块化第1阶段在独立功能分支开发，通过Pull Request进入`develop`，不直接修改`main`。
* 本次为V1.3.8开发副本安全修复候选，尚未形成正式发布版本。
* 默认凭据安全修复本身未改变版本；随后在同一开发中区段完成了V1.3.8版本语义最小统一。
