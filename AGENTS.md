# AGENTS.md

## 1. 项目身份

本工作区用于长期维护“儿科药物性严重过敏反应动态分支虚拟仿真训练引擎”。

系统规划包含临床模式与学院模式，并可能包含训练、考核、评分、问卷、数据登记、管理端和数据导出等模块。

当前实际版本、已实现功能和代码结构必须以以下文件及实际代码为准，不得根据本文件自行推断：

* `docs/CURRENT_STATUS.md`
* `docs/VERSION_HISTORY.md`
* `docs/DECISIONS.md`
* `CHANGELOG.md`
* 当前工作区实际代码

## 2. 默认交流方式

1. 默认使用简体中文与用户交流。
2. 用户不是专业程序员，操作说明必须具体、分步骤、可直接执行。
3. 不使用模糊表述掩盖问题。
4. 无法确认的内容必须明确标记为“未确认”，不得自行编造。
5. 医学、护理、教学、评分、数据和程序问题应分别说明，不得混在一起含糊处理。

## 3. 开始任务前的读取顺序

开始任何涉及项目分析或修改的任务前，按相关性读取：

1. `AGENTS.md`
2. `README.md`
3. `docs/PROJECT_CONTEXT.md`
4. `docs/CURRENT_STATUS.md`
5. `docs/DECISIONS.md`
6. `docs/VERSION_HISTORY.md`
7. `docs/DATA_DICTIONARY.md`
8. `docs/TEST_CHECKLIST.md`
9. `docs/TODO.md`
10. `CHANGELOG.md`

涉及医学逻辑时，还必须读取：

* `references/README.md`
* `references/医学依据索引.md`
* `references/` 中相应原始指南、共识或研究资料

涉及具体实现时，必须继续检查实际代码、配置、数据结构和测试，不得只依据文档判断。

## 4. 项目核心约束

### 4.1 双模式约束

1. 临床模式与学院模式共享医学核心，但面向人群、任务负荷、提示方式、错误反馈和评分重点可以不同。
2. 不得为了开发学院模式而破坏、删除或降级临床模式。
3. 学院模式病例和分支不得机械复制临床模式。
4. 学院模式应采用教学导向设计，包括：

   * 减少不适合护生的角色任务；
   * 使用中性按钮文字，避免直接提示正确答案；
   * 将错误分支设计为可解释的教学反馈；
   * 重点评价识别、呼救、初始处理、抢救配合、复评和沟通。
5. 修改共享医学核心时，必须检查临床模式和学院模式是否同时受到影响。

### 4.2 版本约束

1. 项目使用连续的主版本线。
2. 不得擅自为学院模式建立脱离主系统的新版本线。
3. 当前基线版本必须从 `docs/CURRENT_STATUS.md` 和实际代码中确认，禁止猜测。
4. 每次正式修改必须明确：

   * 修改前版本；
   * 目标版本；
   * 修改范围；
   * 兼容性风险；
   * 回滚方式。
5. 不得覆盖原始版本包。
6. 原始压缩包应保存在 `source_packages/`，历史包保存在相应 `archive/`。
7. 可交付版本应放入 `releases/`，不得与原始包混放。

### 4.3 医学安全约束

1. 医学内容必须能追溯到 `references/` 中指定的指南、专家共识、研究资料或用户确认的院内规范。
2. 不得编造诊断标准、药物剂量、给药途径、操作流程、观察时间或参考文献。
3. 医学资料与现有系统逻辑不一致时，不得静默修改；必须先列出：

   * 当前系统逻辑；
   * 资料依据；
   * 差异；
   * 风险；
   * 建议处理方式。
4. 肾上腺素、液体复苏、气道管理、心肺复苏、特殊人群处理等高风险内容，修改后必须进行专项核查。
5. 医学内容存在歧义或证据不足时，应标记为“需临床专家确认”。
6. 不得将教学模拟系统描述为可替代临床判断或真实诊疗的工具。

### 4.4 数据与权限约束

1. 临床端和学院端的数据、访问范围和管理权限必须明确区分。
2. 管理者原则上只能查看其授权医院、科室或学院的数据。
3. 不得在前端代码、公开配置或说明文档中存放真实密码、密钥、访问令牌或患者身份信息。
4. 不得未经确认修改数据库字段名称、字段类型、主键、访问码规则或历史数据结构。
5. 修改数据结构前必须检查：

   * 旧数据兼容性；
   * 提交流程；
   * 历史记录读取；
   * 统计；
   * 筛选；
   * 导出；
   * 权限控制。
6. 不得将真实患者可识别信息写入测试数据。

## 5. 代码修改原则

1. 默认采用最小必要修改。
2. 不因局部问题进行无关的大规模重构。
3. 修改前先定位：

   * 问题表现；
   * 触发条件；
   * 调用链；
   * 数据流；
   * 影响模块；
   * 可能的回归风险。
4. 不得只修改界面而忽略后端、数据库、评分、历史读取、统计或导出。
5. 不得只根据文件名判断功能，必须检查实际调用关系。
6. 不得删除看似未使用的文件，除非已完成全局检索并确认不存在动态调用、配置引用或构建依赖。
7. 不得用假数据、硬编码返回值或隐藏错误提示的方式伪装问题已经修复。
8. 修复完成后必须执行与修改范围相匹配的验证。
9. 无法执行测试时，必须说明：

   * 未执行的测试；
   * 原因；
   * 潜在风险；
   * 用户应如何人工验证。

## 6. 文件操作规则

1. 未经用户明确要求，不删除任何原始文件。
2. 未经用户明确要求，不覆盖原始压缩包。
3. 解压原始包时应解压到独立目录。
4. 不在原始包解压目录中直接进行不可逆修改。
5. 生成临时文件时优先放入 `working/`。
6. 测试脚本和测试资料放入 `tests/`。
7. 发布文件放入 `releases/`。
8. 修改文档时不得虚构已经实现的功能。

## 7. Git规则

除非用户明确要求：

1. 不初始化 Git。
2. 不执行 `git add`。
3. 不执行 `git commit`。
4. 不执行 `git push`。
5. 不执行强制推送。
6. 不删除分支或标签。
7. 不重写提交历史。
8. 不连接或修改远程仓库。
9. 不把密码、密钥、令牌、患者信息或敏感数据加入版本控制。

用户仅要求“检查”“分析”或“修复代码”时，不代表允许提交或推送。

## 8. 高风险操作

下列操作执行前必须明确说明影响范围，并获得用户明确授权：

* 删除文件或目录；
* 覆盖原始版本；
* 批量重命名；
* 大规模重构；
* 修改数据库结构；
* 清理历史数据；
* 修改权限或访问码体系；
* 安装或升级核心依赖；
* 初始化或修改 Git；
* 发布部署；
* 向外部服务上传项目文件或数据。

## 9. 文档维护

正式修改完成后，根据实际情况更新：

* 当前状态：`docs/CURRENT_STATUS.md`
* 已确认决策：`docs/DECISIONS.md`
* 版本历史：`docs/VERSION_HISTORY.md`
* 数据字段：`docs/DATA_DICTIONARY.md`
* 测试项目：`docs/TEST_CHECKLIST.md`
* 待办事项：`docs/TODO.md`
* 变更记录：`CHANGELOG.md`

不得为了形式一次性改动所有文档。只更新与本次任务直接相关的文件。

## 10. 任务执行流程

### 10.1 分析任务

1. 读取相关文档。
2. 检查实际文件或代码。
3. 区分已确认事实、合理推断和未确认事项。
4. 输出问题、证据、风险和建议。
5. 不进行未经要求的修改。

### 10.2 修改任务

1. 确认当前基线。
2. 定位问题。
3. 说明拟修改文件和影响范围。
4. 实施最小必要修改。
5. 执行测试或人工验证。
6. 检查临床模式、学院模式及共享模块的回归风险。
7. 更新必要文档。
8. 汇报结果。

### 10.3 接管新版本包

1. 保留原始压缩包。
2. 记录文件名、文件大小和接收时间。
3. 解压到独立目录。
4. 扫描目录结构。
5. 确认入口文件、运行方式、依赖和数据存储方式。
6. 对照现有文档核查实际版本。
7. 在未完成接管审计前，不进行功能修改。

## 11. 完成任务后的汇报格式

每次完成分析或修改后，至少报告：

1. 本次任务；
2. 已完成事项；
3. 修改的文件；
4. 未修改的关键文件；
5. 验证或测试项目；
6. 验证结果；
7. 已知限制与遗留风险；
8. 是否更新文档；
9. 是否执行 Git 操作；
10. 建议的下一步。

涉及代码修改时，应尽量提供具体文件路径和关键位置。

## 12. 当前工作区状态

当前工作区仅完成项目目录骨架，尚未导入正式项目代码和原始版本包。

在正式代码导入前：

1. 不得推断实际技术栈。
2. 不得推断页面、模块、数据库或部署方式。
3. 不得创建示例实现冒充项目代码。
4. 不得填写未经验证的当前版本和功能状态。
5. 仅根据用户明确要求建设项目管理文档和工作规范。
