# Copy these values into Streamlit Cloud → App settings → Secrets.
# Do NOT commit real secrets to GitHub.

APP_ACCESS_CODE = ""
ADMIN_PASSWORD = ""

SUPABASE_URL = ""
SUPABASE_SERVICE_ROLE_KEY = ""
SUPABASE_TABLE = "training_records"

# Runtime mode: production or competition. Defaults to production.
APP_MODE = "production"

# Competition-only credentials. Keep empty in this example.
COMPETITION_REVIEW_CODE = ""
COMPETITION_ADMIN_CODE = ""
PEDSIM_COMPETITION_RESULTS_DIR = ""

# Deprecated compatibility flag. Use APP_MODE for new deployments.
PEDSIM_PUBLIC_REVIEW_MODE = false
PEDSIM_DEFAULT_INSTITUTION = ""
AUTH_CONTEXT_SIGNING_KEY = ""

# Unit-admin records must contain only salted credentials. Generate them with:
# python tools/generate_org_admin_credential.py --format toml ...
# Then paste one or more [[ORG_ACCESS_RECORDS]] blocks into deployment secrets.
